#include <stdio.h>

int main(int argc, char **argv)
{
	FILE *build_file = NULL;
	
	// Default case when no file name is specified
	if (argc == 1)
	{

	}
	else if (argc == 2)
	{
		// Whenever you provide a name for your build scipt 
		// even if you don't really need to use this function :)
				
	}
	else // Final case when you provide something with an error 
	{

	}	

	return 0;
}
